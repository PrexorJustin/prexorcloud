manifestVersion: 1
id: backup-orchestrator
version: 1.0.0
hosts: [controller]
backend:
  controller:
    entrypoint: me.prexorjustin.prexorcloud.modules.backup.platform.BackupOrchestratorModule
storage:
  mongo: true
  limits:
    mongoDocuments: 50000
capabilities:
  requires:
    - id: prexor.instance.files
      versionRange: ">=1.0.0 <2.0.0"
