<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!--
      Minimal bootstrap config only. The real appenders (console + rolling FILE) are installed
      programmatically by LoggingSetup.configure(), which calls context.reset() and rebuilds them —
      anything declared here beyond this console fallback would be wiped on startup anyway.
    -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{0} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
